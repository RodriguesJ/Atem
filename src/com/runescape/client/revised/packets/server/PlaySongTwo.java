package com.runescape.client.revised.packets.server;

public class PlaySongTwo {
	/**
	 * if(pktType == 121)
		{
			int j2 = inStream.method436();
			int k10 = inStream.method435();
			if(musicEnabled && !lowMem)
			{
				nextSong = j2;
				songChanging = false;
				onDemandFetcher.method558(2, nextSong);
				prevSong = k10;
			}
			pktType = -1;
			return true;
		}
	 */
}
